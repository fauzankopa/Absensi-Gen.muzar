rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Generus, Absensi, Jurnal Materi, Settings collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
